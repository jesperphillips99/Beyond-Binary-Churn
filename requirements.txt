# Python dependencies for "Beyond Binary Churn".
# Versions are pinned to the environment used to produce the reported results.
# A recent CPU-only install of these packages reproduces every notebook; a CUDA
# build of PyTorch is optional and only speeds up Classify_03 (deep learning).

numpy==2.4.6
pandas==3.0.3
pyarrow==24.0.0
scipy==1.17.1
scikit-learn==1.8.0
statsmodels==0.14.6

lightgbm==4.6.0
xgboost==3.2.0
catboost==1.2.10

torch==2.12.0

matplotlib==3.10.9
seaborn==0.13.2
joblib==1.5.3
tqdm==4.67.3

# Notebook runtime
jupyterlab==4.5.7
notebook==7.5.6
ipykernel==7.2.0
